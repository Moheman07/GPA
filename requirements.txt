# Ultra-lightweight requirements for GitHub Actions (Fast installation)
# Core dependencies only - avoids heavy compilation packages

# Essential data analysis
yfinance>=0.2.12
pandas>=1.5.0
numpy>=1.21.0

# Machine learning (lightweight)
scikit-learn>=1.1.0
joblib>=1.2.0

# HTTP and async (essential for news fetching)
aiohttp>=3.8.0
requests>=2.28.0

# Basic plotting (lightweight)
matplotlib>=3.5.0

# GitHub Actions compatibility
setuptools>=65.0.0
wheel>=0.37.0

# Optional heavy packages (commented out for speed):
# xgboost>=1.6.0        # 223 MB - heavy but good for ML
# textblob>=0.17.1      # NLP with dependencies  
# spacy>=3.4.0          # 31 MB + blis compilation (very slow)
# seaborn>=0.11.0       # Additional plotting

# Note: This configuration provides:
# - Basic technical analysis (RSI, MACD, Moving Averages)
# - Market data fetching (yfinance)
# - Simple ML models (scikit-learn)
# - HTTP requests for news (aiohttp/requests)
# - Basic plotting capabilities
# 
# Estimated installation time: 1-3 minutes (vs 5-10 minutes for full setup)
